
---

# Сборка YTDisBystro на основе zapret для Windows - Zapret от KDS

> Простая и мощная сборка для обхода блокировок YouTube, Discord и других сайтов, заблокированных по различным причинам (через DPI, DNS, списки РКН и т.д.)

---

## ⚠️ ВАЖНО!

То, что вы скачали — **это архив zip**, а не папка! Хотя он и отображается как папка на вашем рабочем столе (или куда вы его скачали).

**Сначала распакуйте папку `YTDisBystro` из архива**, и только потом запускайте `.cmd` файлы.

---

## ⚠️ ВАЖНО-2!

Сборка **не содержит исполняемых файлов**, кроме старого `unzip` 2004 года выпуска и утилиты `RBTray` 2011 года (`https://sourceforge.net/projects/rbtray/files/`).  
Она их скачивает непосредственно с GitHub автора `zapret`. Все свои претензии про "вирусы" можете направлять напрямую автору `zapret`.

Для вирусо-параноиков: рекомендуется удалить программу и искать другие способы смотреть YouTube и ходить на запрещённые сайты.

Или же...

Вы можете **вручную скачать релиз v71** (на момент написания инструкции) с расширением `.ZIP` с GitHub автора `zapret`:  
👉 [https://github.com/bol-van/zapret/releases](https://github.com/bol-van/zapret/releases)

Положите этот файл в папку `YTDisBystro`, и тогда `!!!get_zapret_first!!!.cmd` распакует нужные файлы сразу из него, ничего не скачивая сам.

---

## ✅ Что умеет сборка?

- Обходить блокировку запрещённых сайтов из реестра РКН
- Разблокировать YouTube (включая заливку видео и стриминг по протоколу RTMPS)
- Разблокировать Discord или прочее, если они (или их компоненты) **не забанены по IP**
- Обнаруживать новые блокировки (не по IP) и применять к ним обход
- Обходить внереестровые блокировки адресов известных хостеров, устраиваемые РКН для борьбы с ВПН-сервисами
- Настраиваться пользователем

---

## ❌ Что не умеет сборка?

- Программа `zapret`, как и `GoodbyeDPI`, **не поможет обойти блокировку** на забугорных сайтах по геолокации.
- Не поможет с сайтами, заблокированными по IP (например, Viber).
- Но в браузере может помочь упомянутый ниже `Censor Tracker`, по той же схеме добавления нужного сайта с блокировкой с той стороны или по IP в список проксируемых.
- Также для ресурсов, забаненных по IP, можно использовать перенаправление на другой IP с помощью файла `hosts`. Но такой IP должен существовать и не быть забанен.
- Для всяких лаунчеров игр и подобного вам поможет только **VPN**!

---

## 📦 Содержание

1. [Подготовка программы к запуску](#-подготовка-программы-к-запуску)
2. [Запуск программы](#%EF%B8%8F-запуск-программы)
3. [Настройка стратегий обхода](#%EF%B8%8F-настройка-стратегий-обхода)
4. [Список исключений для сайтов](#-список-исключений-для-сайтов)
5. [Добавление своих сайтов](#-добавление-своих-сайтов)
6. [Решение проблемы 59 секунд из-за бана Google](#-решение-проблемы-59-секунд-из-за-бана-google)
7. [Программа AdGuard и поломанный голос в Discord](#-adguard-и-поломанный-голос-в-discord)
8. [Прочие вопросы](#-прочие-вопросы)
9. [Спасибы и благодарности](#-спасибы-и-благодарности)
10. [Отблагодарить автора](#-отблагодарить-автора)

---

## 🛠 Подготовка программы к запуску

### 1.1
Выполнить `3_service_remove.cmd` во избежание различных проблем с драйвером изменения сетевых пакетов WinDivert (да, его службу этот cmd тоже останавливает и удаляет — он не только для службы `zapret`/`goodbye`).  
❗ Для последних версий сборки этот пункт неактуален — там `cmd` сам всё почистит, что нужно.

### 1.2
В сборке есть возможность задать версию `zapret`, которая будет скачиваться скриптом. Это делается с помощью переменной:
```bat
set zapret_ver=vXX.X
```
Где `XX.X` — версия `zapret`. На момент написания — `v69.9`. Дальше будут версии `v70`, `v70.1` и т.д.

### 1.3
Запустить `!!!get_zapret_first!!!.cmd` — он скачает нужные файлы с GitHub автора `zapret` — можно пользоваться.

#### 1.3.1
Для Windows ниже 10-й версии, где нет встроенного `curl`, можно взять его, например, из GoodChecker или скачать с официального сайта:  
👉 [https://curl.se/windows/](https://curl.se/windows/)  
И положить нужные файлы (`curl.exe`, `curl-ca-bundle.crt`, `libcurl-x64(32).dll`) соответствующей разрядности в `Windows\System32` — в хозяйстве пригодится.

### 1.4
**Пользователям Windows 7, 8 и 8.1 x64:**  
Используйте драйвер WinDivert (2 файла — `dll` и `sys`) из папки `x86_64 goodbyedpi`, заменив имеющиеся здесь, иначе может ругаться и просить цифровую подпись.  
Не забудьте запустить от администратора `3_service_remove.cmd` перед заменой файлов!  
Или запустите `!!!Win7-8_Change_WinDivert!!!.cmd` после скачивания файлов `zapret` — скрипт всё сделает сам.  

Для Windows x86 (32-битных) данное действие **НЕ ТРЕБУЕТСЯ**.

### 1.5 ⚠️ ВАЖНО!
Перед любыми манипуляциями с исполняемыми файлами (удаление перед обновлением, например) или удалением папки сборки — **запускайте `3_service_remove.cmd`**, который выгрузит все используемые файлы из памяти и удалит запись в реестре Windows о WinDivert (драйвер изменения сетевых пакетов).  
Нам тут очередных дурачков с криками «Майнииииир!!!111» из-за биткоин-кошелька для донатов в описании файла не надо :)

### 1.6
При обновлении с предыдущей версии `zapret`/**сборки КРАЙНЕ ЖЕЛАТЕЛЬНО перезагрузить компьютер**, чтобы удалить из памяти закэшированные `desync profiles`.

---

## ▶️ Запуск программы

### 2.1
Запустите `1_preset_russia.cmd` — для использования с окном, с возможностью сворачивания в трей (для сворачивания в трей — кликните **правой кнопкой мыши** на стандартной кнопке "Свернуть").

#### 2.1.1
❌ **Закрывать окно `zapret` (со значком замочка), которое откроется после запуска `1_preset_russia.cmd` — НЕЛЬЗЯ!**  
Обход просто перестанет работать. Логично, да? )

### 2.2
Запустите `2_install_reinstall_service.cmd` (**от администратора!**) для установки как службы.  
Или вариант `_zl`, если хотите настройки как в `zapret launcher`.  
Не запускайте сразу службу, пока не убедитесь в полной работоспособности конфигурации из `1_preset_russia.cmd`.

### 2.3
Включите обратно QUIC и Kyber в браузере:
- В Chrome-подобных — вернуть на `Default`
- В Firefox и его форках — переключить на `true` ключи, найденные по поиску `http3.enable` и `kyber` в `about:config` (если ничего не нашлось — значит нормально, этого нет)

### 2.4
В браузере отключите **все расширения** для проксификации трафика:
- Юбусты
- Фастпрокси
- Цензор Трекеры
- Обходы блокировок Рунета
- ВПНы
- Ссылки на PAC в настройках прокси и подобное

Также отключите все ВПН вне браузера. Перезапустите браузер.

### 2.5
Проверьте работу YouTube, Discord, чего вам ещё нужно...  
Желательно проверять сайты не в одном браузере — видели мы уже тут случаи...

---

## ⚙️ Настройка стратегий обхода

### 3.1
В сборке в `1_preset_russia.cmd` и `2_service_install_reinstall.cmd` имеется возможность настраивать различные стратегии обхода для YouTube (с QUIC и без) и Discord.

### 3.2
Для настройки стратегий откройте `.cmd` файл для изменения:
- ПКМ → Изменить (или «Изменить в Блокноте»)
- Файл откроется в Блокноте

### 3.3
Ориентируясь на подсказки внутри `.cmd`, раскомментируйте нужную запасную стратегию, убрав `rem` и пробел после `rem`.

Пример:
```bat
rem set YTDB_WinSZ
```
заменить на:
```bat
set YTDB_WinSZ
```

### 3.4
После правки сохраните изменённый `.cmd` (Файл → Сохранить) и перезапустите `zapret`:
- С окном — просто закройте окно и снова запустите `1_preset_russia.cmd`
- Как служба — выполните от админа `3_service_remove.cmd`, затем от админа `2_service_install_reinstall.cmd`

### 3.5
`set` — команда командной строки, которая задаёт переменную, через которую параметры (стратегия) обхода передаются непосредственно в программу `zapret` при её запуске.  
Если несколько `set` для одной переменной — сработает только **самое нижнее** значение.

### 3.6
Также можно редактировать фейки в стратегиях:
- Для стратегий с QUIC — `quic_X.bin` (X от 1 до 3)
- Для стратегий TLS — `tls_clienthello_X.bin` (X от 1 до 4)

Иногда удается заставить работать вроде бы «нерабочую» стратегию, просто сменив в ней фейк (если он, конечно, там есть).

### 3.7
В сборке также можно добавить свою стратегию (неважно, откуда вы её взяли), соблюдая формат.  
Не указывайте правила типа `--fw-*` и `--filter-*` (они уже заданы), и, в некоторых случаях, хостлисты/ипсеты, на основе которых стратегия будет применяться.

---

## 🚫 Список исключений для сайтов

### 4.1
Существует список исключений для сайтов (доменов), которые программой обхода **обрабатываться не будут**, ни при каких обстоятельствах.

### 4.2
Находится в файле `lists/netrogat.txt`.

### 4.3
Можно добавлять домены, чтобы исключить их из обработки программой. Например, добавление `gosuslugi.ru` исключит из обработки `gosuslugi.ru` и `*.gosuslugi.ru` (все поддомены третьего уровня сайта Госуслуг).

### 4.4
В этом же файле находятся домены трекеров Rutracker и NNMClub.to, так как они используют технологию Encrypted Client Hello (ECH). При включенном DNS через HTTPS (DoH) никакой обход по их доменам не нужен.

### 4.5 ⚠️ Важно!
Этот файл **не должен оставаться пустым**, ибо тогда сломаются вообще все стратегии обхода! Хотя бы один домен (даже несуществующий, типа `123.xxx`) там всегда должен быть.

---

## 🌐 Добавление своих сайтов

### 5.1
Добавляемые пользователем сайты (домены) добавляются в файл `lists/myhostlist.txt`.  
Желательно в середину списка, чтобы не сбилось форматирование.

### 5.2
Добавление осуществляется без указания протокола (`http`/`https`) и прочих символов (`*` и т.п.).

### 5.3
Пример:  
Чтобы добавить все поддомены третьего уровня для `discordapp.net`, добавьте просто `discordapp.net`. После этого разблокировка будет работать и на `discordapp.net`, и на любом его поддомене 3-го уровня (`***.discordapp.net`).

### 5.4
Если нужно добавить конкретный поддомен — добавляйте его целиком. Например: `vasya.user.com`.  
Тогда разблокировка будет работать на `vasya.user.com`, но не будет работать на `user.com`.

### 5.5
Если стратегия для `myhostlist.txt` не разблокирует какой-нибудь сайт, то его можно добавить в `autohostlist.txt` (**не забудьте убрать из всех других листов!**).  
Хотя в большинстве подобных случаев сайт попадёт туда сам, если заблокирован.  
Если и в этом случае сайт не открывается — значит он забанен по IP (как Инста, например), и нужны другие ухищрения.  
Google / Яндекс вам в помощь.

---

## 🕒 Решение проблемы 59 секунд из-за бана Google

### 6.1
Некоторые пользователи словили бан от Google из-за использования ВПН, прокси и прочего.  
Выражается в том, что сервер Google выдаёт ошибку `403 Forbidden` на два важных запроса, связанных с YouTube.  
Итог: видео не загружается дальше 59 секунд.

### 6.2
Лечится сменой вашего забаненного Google IP через расширение браузера `Censor Tracker`.

### 6.3
Живёт оно здесь:  
👉 [https://censortracker.org/](https://censortracker.org/)  
Выбирайте ссылку под свой браузер (Edge, Яндекс — это тоже Chrome).

### 6.3.1
Продвинутым: ❌ **Не используйте** проксирование через Tor — это не поможет, и даже может стать хуже!  
YouTube Tor очень не любит.

### 6.4
После установки расширения нажмите на мордочку медведя и в открывшемся окне тыкните на шестёрку — откроется страница настроек расширения.

### 6.5
Перейдите в **Регион / Registry of Blocked Resources** и выберите из выпадающего списка **Россия** вместо Автоматически.  
Затем чуть выше отключите ползунок — нам этот функционал не нужен.

### 6.6
Вернитесь на предыдущую страницу настроек и перейдите в **Список проксируемых сайтов / Proxying List**.  
В пустое поле вставьте сайт `jnn-pa.googleapis.com` и нажмите **Сохранить изменения / Save changes**.  
Сайт в поле сократится до `googleapis.com` — это нормально.

### 6.7
Перезапустите браузер.  
Перед закрытием желательно закрыть все вкладки с YouTube, чтобы после перезапуска браузер туда сразу не полез и успел подключиться прокси Цензор Трекера.

### 6.8
После перезапуска браузера проверьте работу YouTube.  
Проблема 59 секунд должна исчезнуть.

### 6.9
Иногда после обновления Censor Tracker настройки могут слететь — придётся делать заново.

---

## 🔊 AdGuard и поломанный голос в Discord

### 7.1
Если вы используете программу AdGuard и у вас подключение к голосу в Discord висит на "Подключение к RTC", выполните следующие действия:

### 7.2
Перейдите в **Настройки > Сеть > Список фильтруемых приложений** и уберите галочку с Discord.

### 7.3
Перезапустите Discord.

---

## ❓ Прочие вопросы

### 8.1
Все вопросы можно задать автору сборки или пользователям на форуме:  
🔗 [https://ntc.party](https://ntc.party) (тема: YTDisBystro)

### 8.2
Почитайте шапку темы или воспользуйтесь поиском по теме, прежде чем задать вопрос — иногда очень помогает.

### 8.3
В шапке темы есть полезные ссылки с инструкциями, например:
- Как включить/настроить DoH в браузере
- Как сменить IP у некоторых заблокированных по IP сайтов через файл hosts

---

## 🙏 Спасибы и благодарности

- ValdikSS — за программу GoodbyeDPI
- bolvan — за замечательную программу zapret
- Ori — за фейк для QUIC и другие идеи
- manwithbox и uwu — за поддержку и общение
- Всем причастным пользователям форума ntc.party — за новые знания, общение и даже неудобные вопросы

---

## 💸 Отблагодарить автора

Отблагодарить автора копеечкой можно по ссылке:  
🔗 [https://pay.cloudtips.ru/p/a5005097](https://pay.cloudtips.ru/p/a5005097)

📌 В комментариях лучше писать просто "Спасибо" или "Спасибо за программу".

---

(c) KDS  
**Удачного вам пользования интернетом без блокировок!**

--- 
